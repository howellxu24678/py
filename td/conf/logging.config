#logger.conf
###############################################
#CRITICAL > ERROR > WARNING > INFO > DEBUG > NOTSET
[loggers]
keys=root,schedule

[logger_root]
level=INFO
handlers=consoleHandler,RotatingFileHander1

[logger_schedule]
handlers=consoleHandler,RotatingFileHander2
qualname=schedule
propagate=0
###############################################
[handlers]
keys=consoleHandler,RotatingFileHander1,RotatingFileHander2

[handler_consoleHandler]
class=StreamHandler
level=INFO
formatter=form02
args=(sys.stderr,)

[handler_RotatingFileHander1]
class=handlers.RotatingFileHandler
level=INFO
formatter=form01
args=('./log/run.log', 'a', 10*1024*1024, 5)

[handler_RotatingFileHander2]
class=handlers.RotatingFileHandler
level=INFO
formatter=form01
args=('./log/schedulers.log', 'a', 10*1024*1024, 5)
###############################################
[formatters]
keys=form01,form02

[formatter_form01]
format=%(asctime)s %(filename)s %(process)d [line:%(lineno)d] %(levelname)s %(message)s
datefmt=%a, %d %b %Y %H:%M:%S

[formatter_form02]
format=%(name)-12s: %(levelname)-8s %(message)s
datefmt=